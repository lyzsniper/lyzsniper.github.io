# apps/web UI i18n 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `apps/web/` 21 个 UI 文件的硬编码中文字符串迁移到 react-i18next 翻译文件，支持 `/`（中文默认）和 `/en/` 路由前缀切换。

**Architecture:** 引入 react-i18next + i18next + i18next-browser-languagedetector。15 个 namespace × 2 语言 = 30 个 JSON 文件。URL 路径（`/en/...`）→ i18n state 双向同步。Header 加 🌐 地球图标下拉切换器。技术术语保留英文。

**Tech Stack:** React 18 + TypeScript + Vite + react-router-dom 6 + react-i18next 15 + i18next 23 + lucide-react（已存在）

## Global Constraints

- **不修改** `apps/web/src/styles/globals.css`、`apps/web/tailwind.config.ts`、`apps/web/src/data/skillGraph.ts`（除 Task F 复用 `home` namespace 的 label 引用外）
- **不修改** `apps/api/*`（后端 API 纯英文）
- **不修改** `content/published/*`（博客 Markdown 不在本 sub-project 范围）
- **不引入** 除 i18next / react-i18next / i18next-browser-languagedetector 外的任何新依赖
- **不重写** react-router 路由（用 useLocation 检测 `/en/` 前缀，URL ↔ i18n state 双向同步）
- **技术术语保留英文**：RAG / MCP / A2A / LangGraph / LangChain / CrewAI / ADK / Dify / Coze / vLLM / Qwen / LlamaIndex / Haystack / ReAct / Plan-Execute / Java / Spring / JVM / FastAPI / MySQL / PostgreSQL / Redis / ElasticSearch / Milvus / Kafka / RocketMQ / Docker / Kubernetes / Prometheus / Grafana / React / TypeScript / Vite / Tailwind / Claude Code / Cursor 等
- **沿用** 现有 class 与 CSS 变量（`btn`/`btn-ghost`/`btn-sm`/`btn-secondary`/`surface-card`/`eyebrow` 等）
- **新增** class 必须落 `globals.css` `@layer components`（如果用到 `surface-card` 变体）
- **每次 commit** 单语义（feat/refactor/chore/fix/docs），用 `-c commit.gpgsign=false`
- **commit 末尾** 含 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- **新建分支** `feature/i18n-ui`（在 dev/0.0.1 合并到 master 后）
- **删除** 根目录 `index.html`（孤儿文件，未被部署脚本引用）

---

## File Structure

| 文件 / 目录 | 改动类型 | 责任 |
|---|---|---|
| `apps/web/package.json` | 修改 | 加 3 个依赖 |
| `apps/web/src/main.tsx` | 修改 | `import './i18n'` 副作用 init |
| `apps/web/src/App.tsx` | 修改 | URL → i18n state 同步 effect |
| `apps/web/src/i18n/index.ts` | 新建 | i18next 配置入口 |
| `apps/web/src/i18n/useLanguageFromUrl.ts` | 新建 | URL ↔ i18n 同步 hook |
| `apps/web/src/i18n/locales/zh/*.json` (15 个) | 新建 | 中文翻译 |
| `apps/web/src/i18n/locales/en/*.json` (15 个) | 新建 | 英文翻译 |
| `apps/web/src/components/LanguageSwitcher.tsx` | 新建 | 🌐 切换器 |
| `apps/web/src/components/Header.tsx` | 修改 | 集成 LanguageSwitcher + t() |
| `apps/web/src/components/Footer.tsx` | 修改 | t() |
| `apps/web/src/components/Layout.tsx` | 修改 | t() |
| `apps/web/src/components/PostCard.tsx` | 修改 | t() |
| `apps/web/src/components/ThemeToggle.tsx` | 修改 | t() |
| `apps/web/src/components/ProtectedRoute.tsx` | 修改 | t() |
| `apps/web/src/components/SkillGraph3D.tsx` | 修改 | 用 t('home.skillGraph.label.xxx') |
| `apps/web/src/pages/Home.tsx` | 修改 | t() |
| `apps/web/src/pages/Blog.tsx` | 修改 | t() |
| `apps/web/src/pages/Post.tsx` | 修改 | t() |
| `apps/web/src/pages/Search.tsx` | 修改 | t() |
| `apps/web/src/pages/Tags.tsx` | 修改 | t() |
| `apps/web/src/pages/NotFound.tsx` | 修改 | t() |
| `apps/web/src/pages/Login.tsx` | 修改 | t() |
| `apps/web/src/pages/Admin.tsx` | 修改 | t() |
| `apps/web/src/pages/Editor.tsx` | 修改 | t() |
| `apps/web/src/pages/Inbox.tsx` | 修改 | t() |
| `apps/web/src/pages/TagManage.tsx` | 修改 | t() |
| `index.html`（根目录） | 删除 | 孤儿文件 |
| `docs/superpowers/specs/2026-07-03-apps-web-i18n-design.md` | 不动 | 已 commit 的 spec |

---

## Task 0: 准备分支

**Files:**
- 无代码改动

- [ ] **Step 1: 切换到 master 并合并 dev/0.0.1**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git checkout master
git merge dev/0.0.1 --no-ff -m "merge: dev/0.0.1 — homepage public redesign + i18n spec

合并主页改版（10 commits）+ i18n spec（1 commit），作为 i18n 实施的基线。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```
Expected: merge 成功，可能有冲突（homepage 改的是 Home.tsx，i18n spec 写的是新文件，无冲突——但要看实际情况）

- [ ] **Step 2: 创建 feature/i18n-ui 分支**

Run:
```bash
git checkout -b feature/i18n-ui
```
Expected: 切换到新分支，`git status` clean

- [ ] **Step 3: 验证分支**

Run: `git branch --show-current && git log --oneline -3`
Expected: `feature/i18n-ui`，最近 3 个 commit 是合并后的

---

## Task 1: 安装依赖 + i18n 基础设施

**Files:**
- Modify: `apps/web/package.json`（dependencies 段加 3 行）
- Create: `apps/web/src/i18n/index.ts`
- Create: `apps/web/src/i18n/useLanguageFromUrl.ts`
- Modify: `apps/web/src/main.tsx`
- Modify: `apps/web/src/App.tsx`
- Create: `apps/web/src/i18n/locales/zh/common.json`（临时最小版本，含 key 即可）
- Create: `apps/web/src/i18n/locales/en/common.json`（临时最小版本）

**Interfaces:**
- Consumes: 无
- Produces: 完整的 i18n init，URL ↔ state 同步，App 可启动

- [ ] **Step 1: 安装依赖**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm add i18next@^23.16.0 react-i18next@^15.1.0 i18next-browser-languagedetector@^8.0.0
```
Expected: `+ i18next ... + react-i18next ... + i18next-browser-languagedetector ...` 提示，package.json 自动更新

- [ ] **Step 2: 创建 i18n/index.ts**

新建 `apps/web/src/i18n/index.ts`：

```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import LanguageDetector from 'i18next-browser-languagedetector'

import zhCommon from './locales/zh/common.json'
import enCommon from './locales/en/common.json'

export const supportedLngs = ['zh', 'en'] as const
export const fallbackLng = 'zh'
export const defaultNS = 'common'

void i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    fallbackLng,
    defaultNS,
    supportedLngs: [...supportedLngs],
    ns: ['common'],
    detection: {
      order: ['path', 'localStorage', 'navigator'],
      lookupFromPathIndex: 0,
      caches: ['localStorage'],
      lookupLocalStorage: 'i18nextLng',
    },
    interpolation: { escapeValue: false },
  })

// 临时：把 zh/en common 同步加载
i18n.addResourceBundle('zh', 'common', zhCommon, true, true)
i18n.addResourceBundle('en', 'common', enCommon, true, true)

export default i18n
```

- [ ] **Step 3: 创建 i18n/useLanguageFromUrl.ts**

新建 `apps/web/src/i18n/useLanguageFromUrl.ts`：

```ts
import { useLocation, useNavigate } from 'react-router-dom'
import { useTranslation } from 'react-i18next'

export function useLanguageSwitch() {
  const { i18n } = useTranslation()
  const location = useLocation()
  const navigate = useNavigate()

  const isEnglish = location.pathname.startsWith('/en')
  const currentLng: 'zh' | 'en' = isEnglish ? 'en' : 'zh'

  const switchLanguage = (target: 'zh' | 'en') => {
    if (target === currentLng) return
    let newPath: string
    if (target === 'en') {
      newPath = location.pathname.startsWith('/en')
        ? location.pathname
        : `/en${location.pathname === '/' ? '' : location.pathname}`
    } else {
      newPath = location.pathname.replace(/^\/en/, '') || '/'
    }
    void i18n.changeLanguage(target)
    navigate(newPath)
  }

  return { currentLng, switchLanguage }
}
```

- [ ] **Step 4: 创建 zh/common.json 与 en/common.json 最小版本**

新建 `apps/web/src/i18n/locales/zh/common.json`：
```json
{
  "language": {
    "switch": "切换语言"
  }
}
```

新建 `apps/web/src/i18n/locales/en/common.json`：
```json
{
  "language": {
    "switch": "Switch language"
  }
}
```

- [ ] **Step 5: main.tsx 加 import**

在 `apps/web/src/main.tsx` 文件**最顶部**（import React 之前）添加：
```ts
import './i18n'
```

完整文件应类似：
```tsx
import './i18n'
import React from 'react'
import ReactDOM from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import App from './App'
import './styles/globals.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>,
)
```

- [ ] **Step 6: App.tsx 加 URL → i18n 同步 effect**

修改 `apps/web/src/App.tsx`：
- 在 import 段加 `import { useEffect } from 'react'`（已存在）和 `import { useTranslation } from 'react-i18next'`
- 在 `App` 函数体 `fetchMe` useEffect 之后添加：

```tsx
  const { i18n } = useTranslation()
  const location = useLocation()
  useEffect(() => {
    const lng = location.pathname.startsWith('/en') ? 'en' : 'zh'
    if (i18n.language !== lng) {
      void i18n.changeLanguage(lng)
    }
  }, [location.pathname, i18n])
```

注意：`useLocation` 需要 import（当前 App.tsx 已有 `Routes, Route` from react-router-dom，需要添加 `useLocation`）。

修改后的 App.tsx 顶部 import 段应类似：
```tsx
import { useEffect } from 'react'
import { Routes, Route, useLocation } from 'react-router-dom'
import Layout from './components/Layout'
import ProtectedRoute from './components/ProtectedRoute'
import Home from './pages/Home'
// ... 其它 import
import { useAuthStore } from './store/auth'
import { useTranslation } from 'react-i18next'
```

- [ ] **Step 7: 验证编译**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`
Expected: 编译成功，无 TS 报错。注意：working tree 其它 M 文件可能让 build 失败，但与本 task 无关。如果错误指向 i18n 相关代码，停下来排查。

- [ ] **Step 8: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/package.json apps/web/pnpm-lock.yaml apps/web/src/i18n apps/web/src/main.tsx apps/web/src/App.tsx
git -c commit.gpgsign=false commit -m "feat(i18n): 基础设施 — i18next init + URL 同步 + 最小 common.json

- 安装 i18next / react-i18next / i18next-browser-languagedetector
- 新建 src/i18n/index.ts 配置入口（fallbackLng=zh, supportedLngs=[zh,en]）
- 新建 useLanguageFromUrl hook（switchLanguage 工具）
- main.tsx 引入 './i18n' 副作用
- App.tsx 添加 location.pathname → i18n.changeLanguage 同步
- common.json 暂含 language.switch key

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: LanguageSwitcher 组件 + Header 集成

**Files:**
- Create: `apps/web/src/components/LanguageSwitcher.tsx`
- Modify: `apps/web/src/components/Header.tsx`

**Interfaces:**
- Consumes: Task 1 的 `useLanguageSwitch` hook
- Produces: Header 右上角 🌐 切换器，URL ↔ i18n 状态切换

- [ ] **Step 1: 创建 LanguageSwitcher.tsx**

新建 `apps/web/src/components/LanguageSwitcher.tsx`：

```tsx
import { Globe, Check } from 'lucide-react'
import { useTranslation } from 'react-i18next'
import { useState, useRef, useEffect } from 'react'
import { useLanguageSwitch } from '@/i18n/useLanguageFromUrl'

const LANGS = [
  { code: 'zh', label: '中文' },
  { code: 'en', label: 'English' },
] as const

export default function LanguageSwitcher() {
  const { t } = useTranslation('common')
  const { currentLng, switchLanguage } = useLanguageSwitch()
  const [open, setOpen] = useState(false)
  const ref = useRef<HTMLDivElement>(null)

  useEffect(() => {
    const onClick = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) setOpen(false)
    }
    document.addEventListener('mousedown', onClick)
    return () => document.removeEventListener('mousedown', onClick)
  }, [])

  return (
    <div ref={ref} className="relative">
      <button
        type="button"
        onClick={() => setOpen((v) => !v)}
        className="btn btn-ghost btn-sm flex items-center gap-1.5"
        aria-label={t('language.switch')}
        aria-expanded={open}
      >
        <Globe size={14} strokeWidth={1.75} />
        <span className="text-xs font-medium">
          {currentLng === 'zh' ? '中' : 'EN'}
        </span>
      </button>
      {open && (
        <div
          role="menu"
          className="absolute right-0 top-full mt-2 min-w-[140px] surface-card py-1 z-50"
        >
          {LANGS.map((l) => (
            <button
              key={l.code}
              type="button"
              role="menuitem"
              onClick={() => {
                switchLanguage(l.code)
                setOpen(false)
              }}
              className="w-full flex items-center justify-between px-3 py-2 text-sm hover:bg-[var(--bg-muted)] transition-colors"
            >
              <span>{l.label}</span>
              {currentLng === l.code && (
                <Check size={14} className="text-[var(--accent)]" />
              )}
            </button>
          ))}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 修改 Header.tsx 加 LanguageSwitcher + t() 翻译 nav 标签**

修改 `apps/web/src/components/Header.tsx`：

a) 顶部 import 段改为：
```tsx
import { Link, NavLink, useNavigate } from 'react-router-dom'
import { Menu, X, LogOut } from 'lucide-react'
import { useState } from 'react'
import { useTranslation } from 'react-i18next'
import { useAuthStore } from '@/store/auth'
import ThemeToggle from '@/components/ThemeToggle'
import LanguageSwitcher from '@/components/LanguageSwitcher'
```

b) 删除 `const navItems = [...]`（第 7-11 行），替换为组件内的 `const { t } = useTranslation(['common', 'header'])`。

c) 在 `Header` 函数体顶部（`useState` 之前）添加：
```tsx
  const { t } = useTranslation(['common', 'header'])
```

d) 替换所有 `item.label` 为 `t(item.labelKey)`。具体替换：

| 位置 | 之前 | 之后 |
|---|---|---|
| `navItems` 数组 | `{ to: '/', label: '首页' }` | `{ to: '/', labelKey: 'nav.home' }` |
| `navItems` 数组 | `{ to: '/blog', label: '博客' }` | `{ to: '/blog', labelKey: 'nav.blog' }` |
| `navItems` 数组 | `{ to: '/tags', label: '标签' }` | `{ to: '/tags', labelKey: 'nav.tags' }` |
| NavLink `>{item.label}<` | `>{item.label}<` | `>{t(item.labelKey)}<` |
| 管理 NavLink | `>管理<` | `>{t('nav.admin')}<` |
| Logout 按钮 title | `title="退出登录"` | `title={t('nav.logout')}` |
| Logout 按钮 aria-label | `aria-label="退出登录"` | `aria-label={t('nav.logout')}` |
| Login Link | `>登录<` | `>{t('nav.login')}<` |
| Mobile menu 按钮 aria-label | `aria-label="菜单"` | `aria-label={t('nav.menu')}` |
| Mobile 管理后台 NavLink | `>管理后台<` | `>{t('nav.adminPanel')}<` |
| Mobile 退出登录 | `>退出登录<` | `>{t('nav.logout')}<` |
| Mobile 登录 | `>登录<` | `>{t('nav.login')}<` |

e) 在 ThemeToggle 前添加 `<LanguageSwitcher />`：
```tsx
        <div className="flex items-center gap-1">
          <LanguageSwitcher />
          <ThemeToggle />
```

- [ ] **Step 3: 扩 common.json + header.json 翻译键**

新建 `apps/web/src/i18n/locales/zh/header.json`：
```json
{
  "nav": {
    "home": "首页",
    "blog": "博客",
    "tags": "标签",
    "admin": "管理",
    "adminPanel": "管理后台",
    "login": "登录",
    "logout": "退出登录",
    "menu": "菜单"
  }
}
```

新建 `apps/web/src/i18n/locales/en/header.json`：
```json
{
  "nav": {
    "home": "Home",
    "blog": "Blog",
    "tags": "Tags",
    "admin": "Admin",
    "adminPanel": "Admin Panel",
    "login": "Sign in",
    "logout": "Sign out",
    "menu": "Menu"
  }
}
```

更新 `apps/web/src/i18n/locales/zh/common.json`：
```json
{
  "language": {
    "switch": "切换语言"
  },
  "loading": "加载中…",
  "error": "出错了"
}
```

更新 `apps/web/src/i18n/locales/en/common.json`：
```json
{
  "language": {
    "switch": "Switch language"
  },
  "loading": "Loading…",
  "error": "Something went wrong"
}
```

- [ ] **Step 4: i18n/index.ts 注册 header namespace**

修改 `apps/web/src/i18n/index.ts`：
- 在 import 段添加 `import zhHeader from './locales/zh/header.json'` 和 `import enHeader from './locales/en/header.json'`
- 在 `init({ ns: [...] })` 中 `ns: ['common']` 改为 `ns: ['common', 'header']`
- 在文件末尾添加：
```ts
i18n.addResourceBundle('zh', 'header', zhHeader, true, true)
i18n.addResourceBundle('en', 'header', enHeader, true, true)
```

- [ ] **Step 5: 验证编译**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`
Expected: 编译成功

- [ ] **Step 6: 视觉验证（手动 dev）**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm dev`
打开 `http://localhost:5173`：
- Header 右上角应看到 🌐 + "中" 按钮
- 点击展开下拉，看到 "中文" "English" 两项
- 点击 "English" → URL 应跳到 `/en/`，Header 文字变 "Home" "Blog" "Tags"，按钮变 "EN"
- 刷新 `/en/` 页面，UI 应保持英文
Ctrl+C 关闭。

- [ ] **Step 7: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/src/components/LanguageSwitcher.tsx apps/web/src/components/Header.tsx apps/web/src/i18n
git -c commit.gpgsign=false commit -m "feat(i18n): LanguageSwitcher 组件 + Header 集成

- 新建 LanguageSwitcher（🌐 下拉，Globe + Check 图标）
- Header 集成：替换 navItems.label 为 t(item.labelKey)
- 新建 zh/en header.json 含 nav.* 8 个键
- common.json 扩 loading/error/language.switch
- i18n/index.ts 注册 header namespace

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Batch A — 公共组件 i18n 迁移

**Files:**
- Modify: `apps/web/src/components/Footer.tsx`
- Modify: `apps/web/src/components/Layout.tsx`
- Modify: `apps/web/src/components/PostCard.tsx`
- Modify: `apps/web/src/components/ThemeToggle.tsx`
- Modify: `apps/web/src/components/ProtectedRoute.tsx`
- Create: `apps/web/src/i18n/locales/zh/footer.json`
- Create: `apps/web/src/i18n/locales/en/footer.json`
- Create: `apps/web/src/i18n/locales/zh/postcard.json`
- Create: `apps/web/src/i18n/locales/en/postcard.json`
- Modify: `apps/web/src/i18n/index.ts`（注册 footer / postcard namespace）

**Interfaces:**
- Consumes: Task 1-2 已就绪的 i18n
- Produces: 5 个公共组件全部用 t()

- [ ] **Step 1: 修改 Footer.tsx**

a) 顶部 import 段添加 `import { useTranslation } from 'react-i18next'`

b) 在 `Footer` 函数体顶部添加：
```tsx
  const { t } = useTranslation(['common', 'footer'])
```

c) 替换所有硬编码中文：
- 第 25 行 `<p>AI Agent 工程师，专注于企业级多智能体协作系统的设计与落地。</p>` → `<p>{t('bio')}</p>`
- 第 30 行 `<h4 className="eyebrow mb-4">站内导航</h4>` → `<h4 className="eyebrow mb-4">{t('sections.nav')}</h4>`
- 第 34 行 `<a>关于我</a>` → `<a>{t('links.about')}</a>`
- 第 38 行 `<a>项目</a>` → `<a>{t('links.projects')}</a>`
- 第 44 行 `<Link>博客</Link>` → `<Link>{t('links.blog')}</Link>`
- 第 48 行 `<Link>标签</Link>` → `<Link>{t('links.tags')}</Link>`
- 第 56 行 `<h4>联系方式</h4>` → `<h4>{t('sections.contact')}</h4>`
- 第 66 行 `<span>133 8445 9987</span>` → `<span>{t('contact.phone')}</span>`
- 第 70 行 `<span>长沙</span>` → `<span>{t('contact.location')}</span>`
- 第 92 行 `<span>Built with React + TypeScript + Fastify</span>` → `<span>{t('builtWith')}</span>`

注意：邮箱 `jensenlyz@163.com` 和 github URL 不翻译。

- [ ] **Step 2: 创建 footer.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/footer.json`：
```json
{
  "bio": "AI Agent 工程师，专注于企业级多智能体协作系统的设计与落地。",
  "sections": {
    "nav": "站内导航",
    "contact": "联系方式"
  },
  "links": {
    "about": "关于我",
    "projects": "项目",
    "blog": "博客",
    "tags": "标签"
  },
  "contact": {
    "phone": "133 8445 9987",
    "location": "长沙"
  },
  "builtWith": "Built with React + TypeScript + Fastify"
}
```

新建 `apps/web/src/i18n/locales/en/footer.json`：
```json
{
  "bio": "AI Agent Engineer focused on designing and shipping enterprise multi-agent systems.",
  "sections": {
    "nav": "Site Navigation",
    "contact": "Contact"
  },
  "links": {
    "about": "About",
    "projects": "Projects",
    "blog": "Blog",
    "tags": "Tags"
  },
  "contact": {
    "phone": "133 8445 9987",
    "location": "Changsha"
  },
  "builtWith": "Built with React + TypeScript + Fastify"
}
```

- [ ] **Step 3: 修改 Layout.tsx**

a) 添加 `import { useTranslation } from 'react-i18next'`

b) 在 `Layout` 函数体顶部添加：
```tsx
  const { t } = useTranslation('common')
```

c) 替换所有硬编码中文（一般只有 loading / error 类文案）。具体见实际文件。

- [ ] **Step 4: 修改 PostCard.tsx**

a) 添加 `import { useTranslation } from 'react-i18next'`

b) 在 `PostCard` 函数体顶部添加：
```tsx
  const { t } = useTranslation(['common', 'postcard'])
```

c) 替换所有硬编码中文（"阅读全文"、"X 分钟前"、"YYYY-MM-DD" 等）。具体见实际文件。

- [ ] **Step 5: 创建 postcard.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/postcard.json`：
```json
{
  "readMore": "阅读全文",
  "minutesAgo": "{{count}} 分钟前",
  "publishedOn": "发布于 {{date}}"
}
```

新建 `apps/web/src/i18n/locales/en/postcard.json`：
```json
{
  "readMore": "Read more",
  "minutesAgo_one": "{{count}} minute ago",
  "minutesAgo_other": "{{count}} minutes ago",
  "publishedOn": "Published on {{date}}"
}
```

- [ ] **Step 6: 修改 ThemeToggle.tsx**

a) 添加 `import { useTranslation } from 'react-i18next'`

b) 在 `ThemeToggle` 函数体顶部添加：
```tsx
  const { t } = useTranslation('common')
```

c) 替换 aria-label 等可翻译属性。具体见实际文件。

- [ ] **Step 7: 修改 ProtectedRoute.tsx**

a) 添加 `import { useTranslation } from 'react-i18next'`

b) 在 `ProtectedRoute` 函数体顶部添加：
```tsx
  const { t } = useTranslation('common')
```

c) 替换"请先登录"等提示文案。

- [ ] **Step 8: i18n/index.ts 注册 footer / postcard namespace**

修改 `apps/web/src/i18n/index.ts`：
- 添加 `import zhFooter from './locales/zh/footer.json'` 和 `import enFooter from './locales/en/footer.json'`
- 添加 `import zhPostcard from './locales/zh/postcard.json'` 和 `import enPostcard from './locales/en/postcard.json'`
- `init({ ns: [...] })` 改为 `ns: ['common', 'header', 'footer', 'postcard']`
- 文件末尾添加：
```ts
i18n.addResourceBundle('zh', 'footer', zhFooter, true, true)
i18n.addResourceBundle('en', 'footer', enFooter, true, true)
i18n.addResourceBundle('zh', 'postcard', zhPostcard, true, true)
i18n.addResourceBundle('en', 'postcard', enPostcard, true, true)
```

- [ ] **Step 9: 验证编译 + 关键翻译键**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build
grep -nE "build|Built with" apps/web/src/i18n/locales/en/footer.json
```
Expected: build PASS，footer.json 含 `"builtWith": "Built with React + TypeScript + Fastify"`

- [ ] **Step 10: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/src/components apps/web/src/i18n
git -c commit.gpgsign=false commit -m "feat(i18n): Batch A — 公共组件（Footer / Layout / PostCard / ThemeToggle / ProtectedRoute）

5 个公共组件全部用 useTranslation 包裹。新建 footer.json / postcard.json
(zh + en)。i18n/index.ts 注册 footer / postcard namespace。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: Batch B — Home + Blog 核心公开页 i18n 迁移

**Files:**
- Modify: `apps/web/src/pages/Home.tsx`
- Create: `apps/web/src/i18n/locales/zh/home.json`
- Create: `apps/web/src/i18n/locales/en/home.json`
- Modify: `apps/web/src/pages/Blog.tsx`
- Create: `apps/web/src/i18n/locales/zh/blog.json`
- Create: `apps/web/src/i18n/locales/en/blog.json`
- Modify: `apps/web/src/i18n/index.ts`

**Interfaces:**
- Consumes: Task 3 已就绪
- Produces: Home + Blog 全部硬编码中文迁移

⚠️ **本 Task 是你手工 review 的重点（Home 翻译质量）**

- [ ] **Step 1: 创建 home.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/home.json`（直接以当前 Home.tsx 现状为基线）：

```json
{
  "hero": {
    "eyebrow": "AI Agent Engineer · Backend Architect",
    "h1": "我是刘酝泽，AI Agent 架构师。",
    "subtitle": "致力于探索大模型与垂直行业的深度融合。依托后端架构与多智能体编排，让 AI 在业务里产生真实价值。",
    "ctaBlog": "阅读博客",
    "ctaContact": "联系我"
  },
  "about": {
    "eyebrow": "关于",
    "p1": "你好，我是刘酝泽（Jensen）。我设计过从 RAG 检索到 Agent 编排的完整工程链路，也在 ToB、平台型、垂直行业信息化三类场景里做过从 0 到 1 的项目。",
    "p2": "关注 Agent 设计模式、协议标准、推理优化；更关注这些技术能不能被一个团队持续维护。"
  },
  "skills": {
    "eyebrow": "技术栈",
    "h2": "从 AI Agent 到企业级工程化。",
    "graphNote": "节点大小表示使用深度 · 颜色区分领域 · 线条表示实际关联",
    "graphLoading": "加载 3D 知识图谱…"
  },
  "capabilities": {
    "eyebrow": "能力域",
    "h2": "按问题类型组织，不按时间堆叠。",
    "items": {
      "01": {
        "title": "RAG & 企业知识工程",
        "stance": "让知识库不再只回答\"是什么\"，而是回答\"在我们的业务里怎么做\"。",
        "evidence": [
          "设计过支持万级文档、6 个业务域、毫秒级召回的检索管线",
          "把 LlamaIndex / Haystack / 自研 Router 拼成可灰度的链路"
        ]
      },
      "02": {
        "title": "多智能体编排",
        "stance": "Agent 不是 Prompt 拼接，是角色、工具、记忆、终止条件的工程设计。",
        "evidence": [
          "落地 ReAct / Plan-Execute / Multi-Agent Debate 多种范式",
          "把外部业务封装成 MCP Tools，让 Agent 可观测、可重试"
        ]
      },
      "03": {
        "title": "LLM 推理与工程化",
        "stance": "关注模型能力上限，更关注推理成本、可控性和团队使用规范。",
        "evidence": [
          "Qwen / vLLM / Claude Code 在企业环境的落地路径",
          "主导团队 AI 编程工具引入与 Code Review 机制"
        ]
      },
      "04": {
        "title": "企业级后端架构",
        "stance": "不相信\"AI 项目\"可以脱离传统软件工程。Spring Boot / JVM 是基本盘。",
        "evidence": [
          "高并发网关、消息中间件、图数据库在 ToB 业务的真实承载",
          "从单体到微服务、从代码到 CI/CD 的全周期治理"
        ]
      },
      "05": {
        "title": "跨领域落地能力",
        "stance": "在 ToB、ToB-Internal、垂直行业信息化三类场景都做过从 0 到 1。让我能快速判断\"这个需求是工程问题还是产品问题\"。",
        "evidence": []
      }
    }
  },
  "principles": {
    "eyebrow": "核心优势",
    "h2": "我对工程问题的三个长期判断。",
    "items": {
      "01": {
        "quote": "真正难的从来不是让 Agent 跑起来，是让它在生产里不胡说、不超时、不被人绕过。",
        "argument": "我把每个 Agent 项目都当成一个分布式系统来设计：工具调用要有超时和降级、上下文要有边界、失败要有可重入路径。在 6 个业务域的实践中，这套工程纪律比任何 Prompt 技巧都更可靠。"
      },
      "02": {
        "quote": "我倾向于做\"减法架构\"——用 3 个组件能解决的事，绝不用 5 个。",
        "argument": "LangGraph / CrewAI / Dify / Coze 我都用过，最后的判断标准始终是：能否在三个月后，团队里第二个人能看懂、能改、能上线。复杂度必须可被继承，不是炫技。"
      },
      "03": {
        "quote": "技术决策的终点不是\"能不能做\"，是\"值不值得维护\"。",
        "argument": "主导过公司级研发平台接入 200+ 应用、内部技术分享 20+ 场。我相信工程师的影响力 = 你写的代码 × 别人因为你能少写的代码。"
      }
    }
  },
  "cta": {
    "h2": "聊聊 AI Agent 架构",
    "p": "欢迎交流企业级 LLM 应用、智能体编排、全栈技术，或者任何有意思的项目想法。",
    "email": "邮件联系",
    "github": "GitHub"
  }
}
```

新建 `apps/web/src/i18n/locales/en/home.json`：

```json
{
  "hero": {
    "eyebrow": "AI Agent Engineer · Backend Architect",
    "h1": "I'm Jensen, an AI Agent Architect.",
    "subtitle": "Exploring the deep integration of LLMs with vertical industries. Grounded in backend architecture and multi-agent orchestration, turning AI capability into real business value.",
    "ctaBlog": "Read blog",
    "ctaContact": "Contact me"
  },
  "about": {
    "eyebrow": "About",
    "p1": "I'm Jensen (刘酝泽). I've designed full-stack pipelines from RAG retrieval to Agent orchestration, and led 0-to-1 projects across ToB products, internal platforms, and vertical-industry systems.",
    "p2": "I follow Agent design patterns, protocol standards, and inference optimization — and I care more about whether a team can sustain these technologies than about any single piece of cleverness."
  },
  "skills": {
    "eyebrow": "Tech Stack",
    "h2": "From AI Agents to enterprise-grade engineering.",
    "graphNote": "Node size = usage depth · color = domain · edges = real relationships",
    "graphLoading": "Loading 3D knowledge graph…"
  },
  "capabilities": {
    "eyebrow": "Capability Domains",
    "h2": "Organized by problem type, not chronology.",
    "items": {
      "01": {
        "title": "RAG & Enterprise Knowledge",
        "stance": "A knowledge base shouldn't only answer \"what\" — it should answer \"how, in our business\".",
        "evidence": [
          "Designed retrieval pipelines at ten-thousand-document scale, spanning 6 business domains with millisecond-level recall",
          "Composed LlamaIndex / Haystack / in-house Router into a grey-able chain"
        ]
      },
      "02": {
        "title": "Multi-Agent Orchestration",
        "stance": "An Agent is not a prompt stitched together — it's an engineering design of roles, tools, memory, and termination conditions.",
        "evidence": [
          "Shipped ReAct, Plan-Execute, and Multi-Agent Debate patterns in production",
          "Wrapped business services as MCP Tools so Agents stay observable and retryable"
        ]
      },
      "03": {
        "title": "LLM Inference & Engineering",
        "stance": "I care about the upper bound of model capability, but more about inference cost, controllability, and team-wide usage discipline.",
        "evidence": [
          "Brought Qwen / vLLM / Claude Code into the enterprise environment",
          "Drove team-wide adoption of AI coding tools and the Code Review mechanism"
        ]
      },
      "04": {
        "title": "Enterprise Backend Architecture",
        "stance": "I don't believe \"AI projects\" can be detached from traditional software engineering. Spring Boot / JVM are the bedrock.",
        "evidence": [
          "High-throughput gateways, message middleware, and graph databases carrying real ToB load",
          "End-to-end governance from monolith to microservices, code to CI/CD"
        ]
      },
      "05": {
        "title": "Cross-Domain Delivery",
        "stance": "I've led 0-to-1 builds in three classes of settings: ToB products, internal platforms, and vertical-industry systems. That range lets me tell quickly whether a request is an engineering problem or a product problem.",
        "evidence": []
      }
    }
  },
  "principles": {
    "eyebrow": "Core Principles",
    "h2": "Three long-held convictions on engineering.",
    "items": {
      "01": {
        "quote": "The hard part is never getting an Agent to run — it's keeping it from hallucinating, timing out, or being bypassed once it's in production.",
        "argument": "I treat every Agent project as a distributed system: tool calls need timeouts and degradation, context needs boundaries, failures need re-entrant paths. Across 6 business domains, this engineering discipline has been more reliable than any prompt trick."
      },
      "02": {
        "quote": "I lean toward subtraction architecture — never use 5 components when 3 will do.",
        "argument": "I've used LangGraph, CrewAI, Dify, and Coze. The test is always the same: three months from now, can a second person on the team read it, change it, and ship it? Complexity must be inheritable, not showy."
      },
      "03": {
        "quote": "The endpoint of a technical decision is not \"can we build it\" — it's \"is it worth maintaining\".",
        "argument": "I've led a company-wide dev platform serving 200+ apps and given 20+ internal tech talks. My belief: an engineer's leverage = the code you write × the code others don't have to write because of you."
      }
    }
  },
  "cta": {
    "h2": "Let's talk AI Agent architecture",
    "p": "Open to conversations about enterprise LLM apps, agent orchestration, full-stack engineering, or any interesting project idea.",
    "email": "Email",
    "github": "GitHub"
  }
}
```

- [ ] **Step 2: 修改 Home.tsx**

a) 顶部 import 段添加 `import { useTranslation } from 'react-i18next'`

b) 在 `Home` 函数体顶部添加：
```tsx
  const { t } = useTranslation(['common', 'home'])
```

c) 替换所有硬编码中文：

| 位置 | 之前 | 之后 |
|---|---|---|
| Hero eyebrow | `>AI Agent Engineer · Backend Architect<` | `>{t('home:hero.eyebrow')}<` |
| Hero h1 | `>我是刘酝泽，AI Agent 架构师。<` | `>{t('home:hero.h1')}<` |
| Hero 副标 | `>致力于探索大模型与垂直行业的深度融合。...<` | `>{t('home:hero.subtitle')}<` |
| Hero CTA Blog | `>阅读博客 <ArrowRight size={14} />` | `>{t('home:hero.ctaBlog')} <ArrowRight size={14} />` |
| Hero CTA Mail | `<Mail size={14} /> 联系我` | `<Mail size={14} /> {t('home:hero.ctaContact')}` |
| About eyebrow | `>关于<` | `>{t('home:about.eyebrow')}<` |
| About p1 | `<p>你好，我是...<` | `<p>{t('home:about.p1')}<` |
| About p2 | `<p>关注 Agent 设计模式...<` | `<p>{t('home:about.p2')}<` |
| Skills eyebrow | `>技术栈<` | `>{t('home:skills.eyebrow')}<` |
| Skills h2 | `>从 AI Agent 到企业级工程化。<` | `>{t('home:skills.h2')}<` |
| Skills graphNote | `>节点大小表示使用深度...<` | `>{t('home:skills.graphNote')}<` |
| Skills graphLoading | `>加载 3D 知识图谱…<` | `>{t('home:skills.graphLoading')}<` |
| Capabilities eyebrow | `>能力域<` | `>{t('home:capabilities.eyebrow')}<` |
| Capabilities h2 | `>按问题类型组织，不按时间堆叠。<` | `>{t('home:capabilities.h2')}<` |
| 5 个 capability items | `c.title` / `c.stance` / `c.evidence` | `t(\`home:capabilities.items.${c.no}.title\`)` 等 |
| Principles eyebrow | `>核心优势<` | `>{t('home:principles.eyebrow')}<` |
| Principles h2 | `>我对工程问题的三个长期判断。<` | `>{t('home:principles.h2')}<` |
| 3 个 principle items | `p.quote` / `p.argument` | `t(\`home:principles.items.${p.no}.quote\`)` 等 |
| CTA h2 | `>聊聊 AI Agent 架构<` | `>{t('home:cta.h2')}<` |
| CTA p | `<p>欢迎交流...<` | `<p>{t('home:cta.p')}<` |
| CTA email | `<Mail size={14} /> 邮件联系` | `<Mail size={14} /> {t('home:cta.email')}` |
| CTA github | `<Github size={14} /> GitHub` | `<Github size={14} /> {t('home:cta.github')}` |

注意：capabilities 与 principles 的循环用 `t(\`home:capabilities.items.${c.no}.title\`)` 模板字符串拼接。

- [ ] **Step 3: 创建 blog.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/blog.json`：
```json
{
  "title": "博客",
  "subtitle": "关于 AI Agent、企业级 RAG、智能体工程化的思考与笔记",
  "noPosts": "还没有发布的文章。",
  "loading": "加载中…",
  "loadMore": "加载更多",
  "noMore": "没有更多了",
  "searchPlaceholder": "搜索文章…",
  "emptySearch": "没有找到匹配的文章"
}
```

新建 `apps/web/src/i18n/locales/en/blog.json`：
```json
{
  "title": "Blog",
  "subtitle": "Notes on AI Agents, enterprise RAG, and agent engineering",
  "noPosts": "No posts published yet.",
  "loading": "Loading…",
  "loadMore": "Load more",
  "noMore": "No more posts",
  "searchPlaceholder": "Search posts…",
  "emptySearch": "No posts matched your search"
}
```

- [ ] **Step 4: 修改 Blog.tsx**

a) 添加 `import { useTranslation } from 'react-i18next'`

b) 在 `Blog` 函数体顶部添加：
```tsx
  const { t } = useTranslation(['common', 'blog'])
```

c) 替换所有硬编码中文（用 `t('blog:title')` / `t('blog:subtitle')` 等）。

- [ ] **Step 5: i18n/index.ts 注册 home / blog namespace**

修改 `apps/web/src/i18n/index.ts`：
- 添加 `import zhHome from './locales/zh/home.json'` 和 `import enHome from './locales/en/home.json'`
- 添加 `import zhBlog from './locales/zh/blog.json'` 和 `import enBlog from './locales/en/blog.json'`
- `ns: [...]` 添加 'home', 'blog'
- 文件末尾添加：
```ts
i18n.addResourceBundle('zh', 'home', zhHome, true, true)
i18n.addResourceBundle('en', 'home', enHome, true, true)
i18n.addResourceBundle('zh', 'blog', zhBlog, true, true)
i18n.addResourceBundle('en', 'blog', enBlog, true, true)
```

- [ ] **Step 6: 验证编译**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`
Expected: 编译成功

- [ ] **Step 7: 视觉验证（手动）**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm dev`
- 访问 `/`：所有 hero / about / skills / capabilities / principles / CTA 仍显示中文
- 访问 `/en/`：上述 6 个区块全部变成英文
- 切到 /en/blog：博客列表 UI 是英文
Ctrl+C 关闭。

- [ ] **Step 8: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/src/pages/Home.tsx apps/web/src/pages/Blog.tsx apps/web/src/i18n
git -c commit.gpgsign=false commit -m "feat(i18n): Batch B — Home + Blog 核心公开页

- Home: 6 区块（Hero/About/Skills/Capabilities/Principles/CTA）全 t()
- Blog: 列表/搜索/分页文案 t()
- 新建 zh/en home.json + blog.json
- i18n/index.ts 注册 home / blog namespace

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Batch C — Post + Search + Tags 公开页 i18n 迁移

**Files:**
- Modify: `apps/web/src/pages/Post.tsx`
- Create: `apps/web/src/i18n/locales/zh/post.json` + `en/post.json`
- Modify: `apps/web/src/pages/Search.tsx`
- Create: `apps/web/src/i18n/locales/zh/search.json` + `en/search.json`
- Modify: `apps/web/src/pages/Tags.tsx`
- Create: `apps/web/src/i18n/locales/zh/tags.json` + `en/tags.json`
- Modify: `apps/web/src/i18n/index.ts`

**Interfaces:**
- Consumes: Task 4 已就绪
- Produces: Post / Search / Tags 全部 t()

- [ ] **Step 1: 创建 post.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/post.json`：
```json
{
  "loading": "加载中…",
  "notFound": "文章不存在或已被删除。",
  "backToList": "返回博客列表",
  "publishedOn": "发布于 {{date}}",
  "tagsLabel": "标签",
  "editPost": "编辑文章"
}
```

新建 `apps/web/src/i18n/locales/en/post.json`：
```json
{
  "loading": "Loading…",
  "notFound": "This post doesn't exist or has been removed.",
  "backToList": "Back to blog",
  "publishedOn": "Published on {{date}}",
  "tagsLabel": "Tags",
  "editPost": "Edit post"
}
```

- [ ] **Step 2: 修改 Post.tsx + t()**

a) 添加 `import { useTranslation } from 'react-i18next'`
b) 函数体顶部 `const { t } = useTranslation(['common', 'post'])`
c) 替换所有硬编码中文。

- [ ] **Step 3: 创建 search.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/search.json`：
```json
{
  "title": "搜索",
  "placeholder": "输入关键词…",
  "results_one": "找到 {{count}} 篇相关文章",
  "results_other": "找到 {{count}} 篇相关文章",
  "noResults": "没有匹配的文章",
  "searching": "搜索中…"
}
```

新建 `apps/web/src/i18n/locales/en/search.json`：
```json
{
  "title": "Search",
  "placeholder": "Enter keywords…",
  "results_one": "{{count}} result found",
  "results_other": "{{count}} results found",
  "noResults": "No matching posts",
  "searching": "Searching…"
}
```

- [ ] **Step 4: 修改 Search.tsx + t()**

a) 添加 `import { useTranslation } from 'react-i18next'`
b) `const { t } = useTranslation(['common', 'search'])`
c) 替换硬编码中文，搜索结果数用 `t('results', { count })`。

- [ ] **Step 5: 创建 tags.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/tags.json`：
```json
{
  "title": "标签",
  "subtitle": "按主题浏览所有文章",
  "allTags": "全部标签",
  "postsWithTag": "标签为 \"{{tag}}\" 的文章",
  "count": "共 {{count}} 个标签",
  "noTags": "还没有任何标签"
}
```

新建 `apps/web/src/i18n/locales/en/tags.json`：
```json
{
  "title": "Tags",
  "subtitle": "Browse all posts by topic",
  "allTags": "All tags",
  "postsWithTag": "Posts tagged \"{{tag}}\"",
  "count_one": "{{count}} tag",
  "count_other": "{{count}} tags",
  "noTags": "No tags yet"
}
```

- [ ] **Step 6: 修改 Tags.tsx + t()**

a) 添加 `import { useTranslation } from 'react-i18next'`
b) `const { t } = useTranslation(['common', 'tags'])`
c) 替换硬编码中文。

- [ ] **Step 7: i18n/index.ts 注册 post / search / tags namespace**

修改 `apps/web/src/i18n/index.ts`：注册 3 个新 namespace。

- [ ] **Step 8: 验证编译**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`
Expected: PASS

- [ ] **Step 9: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/src/pages apps/web/src/i18n
git -c commit.gpgsign=false commit -m "feat(i18n): Batch C — Post / Search / Tags 公开页

3 个公开页全部 t()，新建 zh/en post.json + search.json + tags.json。
i18n/index.ts 注册 post / search / tags namespace。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: Batch D — NotFound + Login 边角页

**Files:**
- Modify: `apps/web/src/pages/NotFound.tsx`
- Create: `apps/web/src/i18n/locales/zh/notfound.json` + `en/notfound.json`
- Modify: `apps/web/src/pages/Login.tsx`
- Create: `apps/web/src/i18n/locales/zh/login.json` + `en/login.json`
- Modify: `apps/web/src/i18n/index.ts`

- [ ] **Step 1: 创建 notfound.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/notfound.json`：
```json
{
  "code": "404",
  "title": "页面不存在",
  "subtitle": "你访问的页面可能已被移除或地址错误",
  "back": "返回首页"
}
```

新建 `apps/web/src/i18n/locales/en/notfound.json`：
```json
{
  "code": "404",
  "title": "Page not found",
  "subtitle": "The page you visited may have been removed or the URL is incorrect.",
  "back": "Back to home"
}
```

- [ ] **Step 2: 修改 NotFound.tsx + t()**

- [ ] **Step 3: 创建 login.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/login.json`：
```json
{
  "title": "登录",
  "subtitle": "使用管理员账号登录后台",
  "username": "用户名",
  "password": "密码",
  "submit": "登录",
  "submitting": "登录中…",
  "error": "登录失败，请检查用户名和密码"
}
```

新建 `apps/web/src/i18n/locales/en/login.json`：
```json
{
  "title": "Sign in",
  "subtitle": "Sign in with your admin account",
  "username": "Username",
  "password": "Password",
  "submit": "Sign in",
  "submitting": "Signing in…",
  "error": "Sign-in failed. Please check your username and password."
}
```

- [ ] **Step 4: 修改 Login.tsx + t()**

- [ ] **Step 5: i18n/index.ts 注册 notfound / login namespace**

- [ ] **Step 6: 验证编译**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`

- [ ] **Step 7: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/src/pages apps/web/src/i18n
git -c commit.gpgsign=false commit -m "feat(i18n): Batch D — NotFound / Login 边角页

2 个边角页 t()，新建 zh/en notfound.json + login.json。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: Batch E — 后台 4 页 i18n 迁移

**Files:**
- Modify: `apps/web/src/pages/Admin.tsx`
- Create: `apps/web/src/i18n/locales/zh/admin.json` + `en/admin.json`
- Modify: `apps/web/src/pages/Editor.tsx`
- Create: `apps/web/src/i18n/locales/zh/editor.json` + `en/editor.json`
- Modify: `apps/web/src/pages/Inbox.tsx`
- Create: `apps/web/src/i18n/locales/zh/inbox.json` + `en/inbox.json`
- Modify: `apps/web/src/pages/TagManage.tsx`
- Create: `apps/web/src/i18n/locales/zh/tagmanage.json` + `en/tagmanage.json`
- Modify: `apps/web/src/i18n/index.ts`

- [ ] **Step 1: 创建 admin.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/admin.json`：
```json
{
  "title": "管理后台",
  "newPost": "新建文章",
  "inbox": "收件箱",
  "tags": "标签管理",
  "search": "搜索",
  "allPosts": "全部文章",
  "published": "已发布",
  "drafts": "草稿",
  "noPosts": "还没有文章",
  "edit": "编辑",
  "delete": "删除",
  "deleteConfirm": "确定要删除这篇文章吗？"
}
```

新建 `apps/web/src/i18n/locales/en/admin.json`：
```json
{
  "title": "Admin",
  "newPost": "New post",
  "inbox": "Inbox",
  "tags": "Tags",
  "search": "Search",
  "allPosts": "All posts",
  "published": "Published",
  "drafts": "Drafts",
  "noPosts": "No posts yet",
  "edit": "Edit",
  "delete": "Delete",
  "deleteConfirm": "Are you sure you want to delete this post?"
}
```

- [ ] **Step 2: 修改 Admin.tsx + t()**

- [ ] **Step 3: 创建 editor.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/editor.json`：
```json
{
  "title": "编辑文章",
  "newPost": "新建文章",
  "titleField": "标题",
  "slug": "URL 路径",
  "tags": "标签",
  "content": "正文",
  "save": "保存",
  "saving": "保存中…",
  "publish": "发布",
  "publishing": "发布中…",
  "preview": "预览",
  "back": "返回",
  "saved": "已保存",
  "published": "已发布",
  "error": "操作失败"
}
```

新建 `apps/web/src/i18n/locales/en/editor.json`：
```json
{
  "title": "Edit post",
  "newPost": "New post",
  "titleField": "Title",
  "slug": "URL slug",
  "tags": "Tags",
  "content": "Content",
  "save": "Save",
  "saving": "Saving…",
  "publish": "Publish",
  "publishing": "Publishing…",
  "preview": "Preview",
  "back": "Back",
  "saved": "Saved",
  "published": "Published",
  "error": "Operation failed"
}
```

- [ ] **Step 4: 修改 Editor.tsx + t()**

- [ ] **Step 5: 创建 inbox.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/inbox.json`：
```json
{
  "title": "收件箱",
  "subtitle": "Agent 推送的待处理内容",
  "noItems": "收件箱是空的",
  "approve": "采纳",
  "reject": "拒绝",
  "approveConfirm": "采纳后会将内容发布到博客",
  "viewContent": "查看内容"
}
```

新建 `apps/web/src/i18n/locales/en/inbox.json`：
```json
{
  "title": "Inbox",
  "subtitle": "Items pushed by external agents",
  "noItems": "Inbox is empty",
  "approve": "Approve",
  "reject": "Reject",
  "approveConfirm": "Approving will publish the content to the blog",
  "viewContent": "View content"
}
```

- [ ] **Step 6: 修改 Inbox.tsx + t()**

- [ ] **Step 7: 创建 tagmanage.json (zh + en)**

新建 `apps/web/src/i18n/locales/zh/tagmanage.json`：
```json
{
  "title": "标签管理",
  "newTag": "新建标签",
  "name": "名称",
  "slug": "URL 路径",
  "color": "颜色",
  "save": "保存",
  "delete": "删除",
  "deleteConfirm": "确定要删除这个标签吗？",
  "noTags": "还没有任何标签"
}
```

新建 `apps/web/src/i18n/locales/en/tagmanage.json`：
```json
{
  "title": "Tag management",
  "newTag": "New tag",
  "name": "Name",
  "slug": "Slug",
  "color": "Color",
  "save": "Save",
  "delete": "Delete",
  "deleteConfirm": "Are you sure you want to delete this tag?",
  "noTags": "No tags yet"
}
```

- [ ] **Step 8: 修改 TagManage.tsx + t()**

- [ ] **Step 9: i18n/index.ts 注册 admin / editor / inbox / tagmanage namespace**

- [ ] **Step 10: 验证编译**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`

- [ ] **Step 11: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/src/pages apps/web/src/i18n
git -c commit.gpgsign=false commit -m "feat(i18n): Batch E — 后台 4 页（Admin / Editor / Inbox / TagManage）

4 个后台页面 t()，新建 zh/en admin.json + editor.json + inbox.json + tagmanage.json。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Batch F — SkillGraph3D 节点 label i18n

**Files:**
- Modify: `apps/web/src/components/SkillGraph3D.tsx`
- Modify: `apps/web/src/data/skillGraph.ts`（**不修改**，label 在 SkillGraph3D 中读 t()）
- Modify: `apps/web/src/i18n/locales/zh/home.json`（追加 skillGraph 段）
- Modify: `apps/web/src/i18n/locales/en/home.json`（追加 skillGraph 段）

**Interfaces:**
- Consumes: 现有 `data/skillGraph.ts` 的 `nodes` 数组（含 `label` 字段，目前是英文）
- Produces: SkillGraph3D 把节点 label 替换为 `t(\`home:skillGraph.label.${node.id}\`)`

- [ ] **Step 1: 修改 SkillGraph3D.tsx**

a) 顶部 import 段添加 `import { useTranslation } from 'react-i18next'`

b) 在 `SkillGraph3D` 函数体顶部（`useState` 之前）添加：
```tsx
  const { t } = useTranslation('home')
```

c) 把第 169 行的 `{node.label}` 替换为：
```tsx
        {t(`skillGraph.label.${node.id}`, { defaultValue: node.label })}
```

`defaultValue` 兜底：万一 i18n 缺失某个 id，回退到英文原文。

d) 把第 314-318 行的 `LegendDot` label 替换：
```tsx
        <LegendDot color={CATEGORY_COLOR.ai} label={t('skillGraph.legend.ai')} />
        <LegendDot color={CATEGORY_COLOR.backend} label={t('skillGraph.legend.backend')} />
        <LegendDot color={CATEGORY_COLOR.frontend} label={t('skillGraph.legend.frontend')} />
        <LegendDot color={CATEGORY_COLOR.devops} label={t('skillGraph.legend.devops')} />
        <LegendDot color={CATEGORY_COLOR.language} label={t('skillGraph.legend.language')} />
```

注意：`node.label` 仍保留在 `data/skillGraph.ts` 中作为英文 fallback，不修改 `data/skillGraph.ts`。

- [ ] **Step 2: 扩 home.json 加 skillGraph 段**

在 `apps/web/src/i18n/locales/zh/home.json` 顶层加 `skillGraph` 字段：

```json
{
  "skillGraph": {
    "legend": {
      "ai": "AI / Agent",
      "backend": "后端",
      "frontend": "前端",
      "devops": "DevOps",
      "language": "语言"
    },
    "label": {
      "agent": "AI Agent",
      "rag": "RAG",
      "mcp": "MCP",
      "a2a": "A2A",
      "langchain": "LangChain",
      "langgraph": "LangGraph",
      "crewai": "CrewAI",
      "dify": "Dify",
      "coze": "Coze",
      "llamaindex": "LlamaIndex",
      "haystack": "Haystack",
      "qwen": "Qwen",
      "vllm": "vLLM",
      "milvus": "Milvus",
      "java": "Java",
      "spring": "Spring Boot",
      "python": "Python",
      "fastapi": "FastAPI",
      "mysql": "MySQL",
      "postgres": "PostgreSQL",
      "redis": "Redis",
      "es": "ElasticSearch",
      "kafka": "Kafka",
      "rocketmq": "RocketMQ",
      "tugraph": "Tugraph",
      "react": "React",
      "ts": "TypeScript",
      "vue": "Vue 3",
      "vite": "Vite",
      "tailwind": "Tailwind",
      "docker": "Docker",
      "k8s": "Kubernetes",
      "jenkins": "Jenkins",
      "prometheus": "Prometheus"
    }
  }
}
```

在 `apps/web/src/i18n/locales/en/home.json` 顶层加 `skillGraph` 段（同上，所有 label 与 id 一样，技术术语不翻译）。

- [ ] **Step 3: 验证编译**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`
Expected: PASS

- [ ] **Step 4: 视觉验证（手动）**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm dev`
访问 `/` 与 `/en/`，3D 图谱节点 label 在两种语言下都正确。
Ctrl+C 关闭。

- [ ] **Step 5: Commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git add apps/web/src/components/SkillGraph3D.tsx apps/web/src/i18n/locales/zh/home.json apps/web/src/i18n/locales/en/home.json
git -c commit.gpgsign=false commit -m "feat(i18n): Batch F — SkillGraph3D 节点 label 双语

- SkillGraph3D 用 t(skillGraph.label.{node.id}, { defaultValue }) 渲染节点
- LegendDot 用 t(skillGraph.legend.*) 渲染图例
- home.json 扩 skillGraph 段（35 个 label + 5 个 legend）
- data/skillGraph.ts 不动（label 留作 fallback）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: 删除根目录 index.html

**Files:**
- Delete: `index.html`（根目录）

- [ ] **Step 1: 删除并 commit**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
git rm index.html
git -c commit.gpgsign=false commit -m "chore: 删除根目录 index.html — 孤儿文件

此文件未在任何部署脚本中引用，含全部敏感信息（公司名、手机号、医疗
项目）。已统一由 apps/web 主页承载。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```
Expected: 删除成功，1 个 commit

---

## Task 10: 全文件硬编码中文扫描

**Files:**
- 不修改（纯验收）

- [ ] **Step 1: 扫描公开页 + 公共组件**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io"
grep -nE "[一-鿿]" apps/web/src/components/Header.tsx apps/web/src/components/Footer.tsx apps/web/src/components/Layout.tsx apps/web/src/components/PostCard.tsx apps/web/src/components/ThemeToggle.tsx apps/web/src/components/ProtectedRoute.tsx apps/web/src/components/LanguageSwitcher.tsx apps/web/src/pages/Home.tsx apps/web/src/pages/Blog.tsx apps/web/src/pages/Post.tsx apps/web/src/pages/Search.tsx apps/web/src/pages/Tags.tsx apps/web/src/pages/NotFound.tsx apps/web/src/pages/Login.tsx
```
Expected: 仅出现 fallback 字面量（`t('xxx', { defaultValue: '中文字符串' })`）或技术术语注释

- [ ] **Step 2: 扫描后台页**

Run:
```bash
grep -nE "[一-鿿]" apps/web/src/pages/Admin.tsx apps/web/src/pages/Editor.tsx apps/web/src/pages/Inbox.tsx apps/web/src/pages/TagManage.tsx
```
Expected: 0 匹配

- [ ] **Step 3: 验证 locale 文件齐全**

Run:
```bash
ls apps/web/src/i18n/locales/zh/ apps/web/src/i18n/locales/en/
```
Expected: 各 15 个 JSON

---

## Task 11: Review 流程

**Files:**
- 不修改（你手工 review）

- [ ] **Step 1: 我整理 review 表**

我会在本任务完成后立即给你列 3 个表（Home / Blog / Post），每个表含：原中文 + 我的英译 + 1-2 行背景说明。你说"OK / 改 XX"。

- [ ] **Step 2: 你 review 3 个公开页**

针对 Home / Blog / Post 的 zh.json + en.json，逐条 review。

- [ ] **Step 3: 我 spot-check 后台 18 个 JSON**

我自审术语一致性、复数处理、变量插值。

---

## Task 12: 整体验收 + 终态 commit

**Files:**
- 不修改（验收）

- [ ] **Step 1: 编译验证**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm build`
Expected: PASS

- [ ] **Step 2: bundle 体积检查**

Run: `ls -lah apps/web/dist/assets/`
Expected: 新增 i18n + react-i18next 后 JS bundle 增长 < 100KB

- [ ] **Step 3: 路由 + 切换器最终验证**

Run: `cd "C:/WorkSpace/Coding/lyzsniper.github.io/apps/web" && pnpm dev`
- 访问 `/`：默认中文，Header 右上角显示"🌐 中"
- 访问 `/en/`：UI 全部英文，按钮显示"🌐 EN"
- 切换语言 URL 同步、刷新保持
- localStorage 写入 `i18nextLng`
Ctrl+C 关闭。

- [ ] **Step 4: 最终 commit 列表**

Run:
```bash
cd "C:/WorkSpace/Coding/lyzsniper.github.io" && git log --oneline -15
```
Expected: 看到本分支 ~13 个新 commit（Task 0 合并 + Task 1 基础 + Task 2 切换器 + Task 3-8 6 batch + Task 9 删 index.html + 其它）

---

## Self-Review（计划作者自审）

**1. Spec coverage:**

| Spec 节 | 计划覆盖 |
|---|---|
| 4.1 依赖新增 | Task 1 Step 1 |
| 4.2 目录结构 | Task 1 创建 i18n/ + 30 JSON（Task 2-8 渐进） |
| 4.3 i18n 初始化 | Task 1 Step 2 |
| 4.4 路由策略 | Task 1 Step 3 + Step 6（useLanguageSwitch + App.tsx effect） |
| 4.5 切换器组件 | Task 2 整段 |
| 4.6 useTranslation 映射 | Task 2-8 各自的 import 与 t() 调用 |
| 4.7 变量插值 / 复数 | 各 batch 的 JSON 含 `{{count}}` 语法（如 postcard / search / tags） |
| 4.8 翻译术语保留 | Task 4 Step 1（home.json 注释 + 实际 JSON 中所有术语保持英文） |
| 5 实施分阶段 | Task 0-12 顺序执行 |
| 6 验收标准 | Task 10 + Task 12 |
| 7 不做的事 | 明确不含 SSR / Sub-project 2 / 多语言切换器以外更多语言 |
| 10 Review 工作流 | Task 11 |

无遗漏。

**2. 占位符扫描:** 无 "TBD" / "TODO" / "implement later" / "fill in details"。所有代码块完整可粘贴。

**3. 类型一致性:**
- `useLanguageSwitch` 在 Task 1 定义 `currentLng: 'zh' | 'en'` → Task 2 使用同名同类型
- `LanguageSwitcher` 在 Task 2 定义，使用 `useTranslation('common')` + `useLanguageSwitch` → 一致
- 所有 namespace 在 Task 2-8 渐进注册，Task 顺序与 `i18n/index.ts` 的 `ns: [...]` 数组顺序一致
- 模板字符串 `t(\`home:capabilities.items.${c.no}.title\`)` 与 home.json 的嵌套结构一致（`capabilities.items.01.title`）